"""ACE proof-of-concept benchmark: generator + runner (see benchmarks/generator.py,
benchmarks/runner.py). Not part of the shipped ace_enterprise package -- this is
tooling used to demonstrate the ACE self-learning loop (RED -> Curate -> GREEN)
against an open-weight model before making the repository public.
"""
